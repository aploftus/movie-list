import React from 'react';

function WatchedFilter(props) {
  return (
    <span className="filter">
      <button className="filter-watched">Watched</button>
      <button className="filter-watched">Watched</button>
    </span>
  )
}

export default WatchedFilter;